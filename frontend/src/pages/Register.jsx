import { Link, useNavigate } from 'react-router-dom'; import { useAuth } from '../hooks/useAuth'
export default function Register() { const { login } = useAuth(); const navigate = useNavigate(); return <main className="auth-page"><section className="auth-panel"><p className="eyebrow">Start your workspace</p><h1>Turn plans into<br /><em>progress.</em></h1><form onSubmit={e => { e.preventDefault(); login(e.target.email.value); navigate('/dashboard') }}><label>Name<input name="name" required /></label><label>Email<input name="email" type="email" required /></label><label>Password<input name="password" type="password" required /></label><button className="primary">Create account</button></form><p className="muted">Already registered? <Link to="/login">Sign in</Link></p></section></main> }