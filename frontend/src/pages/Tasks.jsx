import { useState } from 'react'; import Sidebar from '../components/Sidebar'; import TaskCard from '../components/TaskCard'; import TaskForm from '../components/TaskForm'; import TaskFilter from '../components/TaskFilter'; import SearchBar from '../components/SearchBar'
export default function Tasks() { const [tasks, setTasks] = useState([]); const [filter, setFilter] = useState('all'); const [search, setSearch] = useState(''); const add = task => setTasks([...tasks, { ...task, id: Date.now() }]); const visible = tasks.filter(t => (filter === 'all' || (filter === 'completed' ? t.completed : !t.completed)) && t.title.toLowerCase().includes(search.toLowerCase())); return <div className="app-shell"><Sidebar /><main className="content"><div className="page-heading"><div><p className="eyebrow">Task library</p><h1>All your <em>moving parts.</em></h1></div></div><TaskForm onSubmit={add} /><div className="toolbar"><SearchBar value={search} onChange={setSearch} /><TaskFilter value={filter} onChange={setFilter} /></div><div className="task-list">{visible.length ? visible.map(task => <TaskCard task={task} onToggle={id => setTasks(tasks.map(t => t.id === id ? { ...t, completed: !t.completed } : t))} key={task.id} />) : <div className="empty">No tasks match this view yet.</div>}</div></main></div> }