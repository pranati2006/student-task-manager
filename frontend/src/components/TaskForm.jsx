import { useState } from 'react'
export default function TaskForm({ onSubmit }) { const [title, setTitle] = useState(''); const [priority, setPriority] = useState('medium'); return <form className="task-form" onSubmit={(e) => { e.preventDefault(); if (title.trim()) { onSubmit({ title, priority, completed: false }); setTitle('') } }}><input value={title} onChange={e => setTitle(e.target.value)} placeholder="What needs doing?" /><select value={priority} onChange={e => setPriority(e.target.value)}><option>low</option><option>medium</option><option>high</option></select><button className="primary">Add task</button></form> }